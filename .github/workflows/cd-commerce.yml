name: CD - Commerce Service

on:
  push:
    branches: [ "devticket-commerce" ]
    paths: [ "commerce/**" ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x ./commerce/gradlew

      - name: Build with Gradle
        run: |
          cd commerce
          ./gradlew clean bootJar

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        run: |
          SHORT_SHA=${GITHUB_SHA::7}
          IMAGE_BASE=${{ secrets.DOCKERHUB_USERNAME }}/devticket-domain-commerce
          docker build -t ${IMAGE_BASE}:${SHORT_SHA} -t ${IMAGE_BASE}:latest ./commerce
          docker push ${IMAGE_BASE}:${SHORT_SHA}
          docker push ${IMAGE_BASE}:latest

      - name: Deploy
        run: |
          IMAGE_URL=${{ secrets.DOCKERHUB_USERNAME }}/devticket-domain-commerce:latest
          docker pull $IMAGE_URL
          docker stop devticket-commerce || true
          docker rm devticket-commerce || true
          docker run -d \
            --name devticket-commerce \
            --network host \
            --restart always \
            -e SPRING_PROFILES_ACTIVE=prod \
            -e DB_URL="jdbc:postgresql://localhost:5432/devticket?currentSchema=commerce" \
            -e DB_USERNAME=${{ secrets.DB_USERNAME }} \
            -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
            -e KAFKA_BOOTSTRAP_SERVERS=localhost:9092 \
            -e JAVA_OPTS="-Xms256m -Xmx512m" \
            $IMAGE_URL
          docker image prune -f

      - name: Health Check
        run: |
          echo "Waiting for commerce service to start..."
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8083/actuator/health > /dev/null 2>&1; then
              echo "Commerce service is healthy!"
              exit 0
            fi
            echo "Attempt $i/30 - waiting 3s..."
            sleep 3
          done
          echo "Commerce service health check failed!"
          docker logs devticket-commerce --tail 50
          exit 1